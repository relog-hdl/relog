#ifndef RELOG_SERVICE_LOGGING_LOGGER_H
#define RELOG_SERVICE_LOGGING_LOGGER_H

namespace relog::service::logging
{

template<typename TMessage>
class Logger;

} // namespace relog::service::logging

#endif
